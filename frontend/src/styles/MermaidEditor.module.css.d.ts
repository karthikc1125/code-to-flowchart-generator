declare const styles: {
  readonly container: string;
  readonly header: string;
  readonly title: string;
  readonly subtitle: string;
  readonly editorContainer: string;
  readonly editorSection: string;
  readonly previewSection: string;
  readonly sectionTitle: string;
  readonly editorWrapper: string;
  readonly previewWrapper: string;
  readonly renderButton: string;
  readonly loadingSpinner: string;
  readonly analysisButton: string;
};

export default styles; 